import 'package:flutter/material.dart';

class NewPostEditView extends StatelessWidget {
  const NewPostEditView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: const Text("Hello")),
    );
  }
}